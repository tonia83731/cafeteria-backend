const { UserCupon } = require("../models");

const cuponController = {
  getUserCupons: async (req, res, next) => {
    try {
    } catch (error) {
      console.log(error);
    }
  },
  applyUserCupon: async (req, res, next) => {
    try {
    } catch (error) {
      console.log(error);
    }
  },
};

module.exports = cuponController;
